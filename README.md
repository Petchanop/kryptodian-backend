before running start intialize database container first:
- make db

open vscode or other ide and run:
- vscode ctrl+p build reopen in container or rebuild

after container run install all dependencies:
- npm install
